return {
  -- Both sections are optional and independent — fill in only the
  -- service(s) you want to use.
  storygraph = {
    session_cookie = '',
    remember_user_token = '',
  },
  hardcover = {
    token = '',
  },
}
